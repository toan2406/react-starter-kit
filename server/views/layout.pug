doctype html
html
  head
    title= title
    script.
      window.__INITIAL_STATE__ = !{initialState}
      window.__SPLIT_POINTS__ = !{splitPoints}
  body
    block content
    script(src='/app.bundle.js')
